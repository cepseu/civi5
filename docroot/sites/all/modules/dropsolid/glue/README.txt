//The glue module contains code that should be available on every dropsolid site
//The module must not be hacked. It should be maintained like a contrib module.
//It contains
* libraries to deploy
* basic automatic tests + custom testcase
* installer
* some overrides we want to have in place
  * protect permissions